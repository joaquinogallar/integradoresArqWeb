services:
  db:
    container_name: journey
    image: postgres:14.4
    environment:
      - POSTGRES_USER=admin
      - POSTGRES_DB=journey
    ports:
      - "127.0.0.1:5432:5432"
    command: postgres -c 'log_statement=all'
